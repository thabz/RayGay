
AC_INIT(src/main.cpp) dnl a source file from your sub dir
AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

AC_SUBST(MAGIC_CFLAGS)
AC_SUBST(MAGIC_LIBS)

PACKAGE=tracer
VERSION=0.1

AC_PROG_CXX
AC_PROG_RANLIB
AC_PROG_LIBTOOL

CPPFLAGS="$CPPFLAGS -O3 -Wall"

AC_OUTPUT(Makefile src/Makefile src/materials/Makefile test/Makefile src/paths/Makefile)
