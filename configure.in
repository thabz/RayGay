
AC_INIT(src/main.cpp) dnl a source file from your sub dir
AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

#MAGIC_CONFIG="/usr/bin/Magick++-config"
#MAGIC_CFLAGS=`/usr/bin/Magick++-config --cxxflags --cppflags | tr "\n" " "`
#MAGIC_LIBS=`/usr/bin/Magick++-config --ldflags --libs | tr "\n" " "`

AC_SUBST(MAGIC_CFLAGS)
AC_SUBST(MAGIC_LIBS)

   
#PKG_CHECK_MODULES(GTKMM, gtkmm-2.0 >= 2.2)
#AC_SUBST(GTKMM_CFLAGS)
#AC_SUBST(GTKMM_LIBS)

PACKAGE=tracer
VERSION=0.1

AC_PROG_CXX
AC_PROG_RANLIB
AC_PROG_LIBTOOL

CPPFLAGS="$CPPFLAGS -O3 -Wall"

AC_OUTPUT(Makefile src/Makefile src/materials/Makefile test/Makefile)
